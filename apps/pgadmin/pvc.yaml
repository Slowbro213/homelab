apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgadmin-data
  namespace: pgadmin
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-rwo
  resources:
    requests:
      storage: 2Gi
