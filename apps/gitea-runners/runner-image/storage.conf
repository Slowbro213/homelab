# vfs: no /dev/fuse, no privileged mounts required. Slower and more disk-hungry
# than overlay, but works in an unprivileged baseline-PodSecurity pod as root.
[storage]
driver = "vfs"
runroot = "/run/containers/storage"
graphroot = "/var/lib/containers/storage"
