apiVersion: v1
kind: Service
metadata:
  name: argocd-tailscale
  namespace: argocd
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app.kubernetes.io/name: argocd-server
