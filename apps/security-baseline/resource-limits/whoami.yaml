apiVersion: v1
kind: LimitRange
metadata:
  name: default-resource-limits
  namespace: apps
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 25m
        memory: 64Mi
      default:
        memory: 256Mi
      max:
        cpu: 500m
        memory: 512Mi
