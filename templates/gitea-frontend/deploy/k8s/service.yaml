apiVersion: v1
kind: Service
metadata:
  name: frontend-template
  namespace: apps
  labels:
    app.kubernetes.io/name: frontend-template
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frontend-template
  ports:
    - name: http
      port: 80
      targetPort: http
