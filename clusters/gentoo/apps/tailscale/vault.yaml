apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-tailscale
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Slowbro213/homelab.git
    targetRevision: main
    path: apps/tailscale/vault
  destination:
    server: https://kubernetes.default.svc
    namespace: vault
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
