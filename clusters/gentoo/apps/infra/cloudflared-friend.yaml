apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudflared-friend
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: infra

  source:
    repoURL: https://github.com/Slowbro213/homelab.git
    targetRevision: main
    path: apps/cloudflared-friend

  destination:
    server: https://kubernetes.default.svc
    namespace: cloudflare-friend

  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
